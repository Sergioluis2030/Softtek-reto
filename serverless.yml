service: my-test-segundo

provider:
  name: aws
  runtime: nodejs18.x

functions:
  hello:
    handler: src/handler.hello
    events:
      - http:
          path: hello
          method: get